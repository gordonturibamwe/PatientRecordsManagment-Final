<%= form_for([@patient, @summary]) do |f| %>
  <% if @summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@summary.errors.count, "error") %> prohibited this summary from being saved:</h2>

      <ul>
      <% @summary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
    <%= f.text_field :complaint, :class => "form-control", :placeholder => "Patient complaint" %>
  </div>
    <textarea class="form-control" rows="3"
    <%= f.text_area :notes, :class => "form-control", :placeholder => "Type in the visitng notes taken from the patient" %>


  <hr>
  <div class="row">
  <div class="col-md-5">
    <%= f.submit :class => "btn btn-success" %> | 
<% end %>

